import { View } from 'react-native';

export default function HorizontalLine() {
  return (
    <View 
      style={{
        borderBottomWidth: 1,
        borderBottomColor: 'black',
        marginTop: 16,
        width: 390
      }}
    />

  );
}

